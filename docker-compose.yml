version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
    depends_on:
      - db

  db:
    image: postgres:15-alpine
    environment:
      POSTGRES_USER: bldchk
      POSTGRES_PASSWORD: Dev0p5!$.
      POSTGRES_DB: baza_mea
    ports:
      - "5432:5432"